#include <sdtio.h>
#include "main.h"

/**
 * main - Print thename of the program
 * @argc: Count arguments
 * @argv: Arguments
 *
 * Return: Always 0 (Success)
 */

int (int argc, char *argv[])
{
int i;
for (i = 0; i < arg; i++)
{
printf("%s\n", argv[i]);	
}
return (0);
}
